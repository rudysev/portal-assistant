<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws the UI; this just gives the Activity a dark, no-action-bar window
         (Portal is a dark countertop device). -->
    <style name="Theme.Assistant" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
